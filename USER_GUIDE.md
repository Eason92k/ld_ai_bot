# 雷電模擬器 AI 機器人 (LD AI Bot) 使用說明書

本系統是一套專為**雷電模擬器 (LDPlayer)** 設計的高效能 AI 自動化機器人工具。支援背景模擬操作、多開同步控制、圖形化邏輯腳本、電腦視覺戰鬥辨識以及自訂技能連招語法與 CD 偵測。

---

## 📖 目錄
1. [系統特點與架構](#1-系統特點與架構)
2. [快速上手指南](#2-快速上手指南)
3. [ GUI 介面四大分頁功能介紹](#3-gui-介面四大分頁功能介紹)
   - [3.1【基本功能】動作錄製與回放](#31-基本功能動作錄製與回放)
   - [3.2【進階腳本】圖形化邏輯腳本設計](#32-進階腳本圖形化邏輯腳本設計)
   - [3.3【技能套組設定】技能連招與 CD 偵測](#33-技能套組設定技能連招與-cd-偵測)
   - [3.4【多開總控】多模擬器集中管理](#34-多開總控多模擬器集中管理)
4. [⚡ 技能指令語法 (Skill DSL) 詳細說明](#4-技能指令語法-skill-dsl-詳細說明)
5. [🧩 進階腳本 11 種步驟類型說明](#5-進階腳本-11-種步驟類型說明)
6. [🚀 多開啟動器 (start_bots.py) CLI 使用指南](#6-多開啟動器-start_botspy-cli-使用指南)
7. [❓ 常見問題與安全注意事項 (FAQ)](#7-常見問題與安全注意事項-faq)

---

## 1. 系統特點與架構

* **背景零干擾操作**：採用 Windows API (`PostMessage`)，模擬器可置於背景、被其他視窗遮擋仍可正常執行點擊、滑動與鍵盤操作，不佔用實體滑鼠鍵盤。
* **背景高清截圖**：採用 `PrintWindow` 技術，即時擷取背景模擬器畫面，供視覺辨識與 CD 判讀。
* **智慧圖像比對 (Smart Mode)**：錄製時自動擷取目標位置圖片範本，播放時確認畫面符合才執行點擊，大幅增強腳本抗延遲能力。
* **高效能視覺分析**：戰鬥與技能判斷採用純 HSV 色彩及亮度矩陣計算（無 OCR 負擔），確保極低 CPU/GPU 資源占用。
* **靈活分支邏輯**：支援「找圖跳轉」、「戰鬥狀態偵測跳轉」、「智能結算」、「關卡完成檢查」等自動化流程控制。

---

## 2. 快速上手指南

### 啟動主程式
在終端機執行以下命令以啟動主圖形介面：
```bash
python main.py
```

### 第一次錄製與播放基本腳本
1. 確定雷電模擬器已開啟。
2. 在 **【基本功能】** 分頁點擊 **「重新整理視窗」**，勾選要操控的模擬器。
3. 在腳本名稱欄輸入名稱（如 `test_script.json`）。
4. 點擊 **「開始錄製」**，在模擬器視窗內進行操作（點擊、拖曳）。
5. 點擊 **「停止錄製」**，腳本將自動儲存於 `scripts/` 資料夾。
6. 設定 **播放次數**，點擊 **「開始播放」** 即可自動執行。
7. 播放中隨時按 **`ESC`** 或 **`F12`** 鍵可立即緊急中斷播放。

---

## 3. GUI 介面四大分頁功能介紹

### 3.1 【基本功能】動作錄製與回放
本分頁用於執行單純的滑鼠動作錄製與重播：

* **模擬器視窗選擇區**：顯示當前已偵測到的雷電模擬器視窗。
* **錄製模式**：
  * **同步錄製 (Sync)**：錄製主視窗動作，播放時同步推送到所有已勾選的模擬器視窗。
  * **獨立錄製 (Independent)**：分別記錄各模擬器個別點擊的座標。
* **Smart 視覺辨識模式 (選用)**：開啟後會在錄製點擊時自動備份周邊截圖（存於 `scripts/<腳本名>_assets/`），播放時比對圖片一致才點擊。
* **播放參數**：
  * **腳本選擇**：從下拉選單直接選擇 `scripts/` 下的 JSON 腳本。
  * **執行次數**：設定迴圈執行次數（設定為高數值可作為長期掛機）。

---

### 3.2 【進階腳本】圖形化邏輯腳本設計
進階分頁允許您建構具有「邏輯分支」與「條件判斷」的自動化流程：

* **動作類型與參數設定**：可選擇點擊、滑動、等待、找圖點擊、條件跳轉、戰鬥偵測等 11 種指令。
* **腳本步驟清單**：直觀顯示所有步驟順序。
* **步驟管理按鈕**：
  * **新增步驟**：將設定好的動作加入腳本末端。
  * **刪除選擇**：刪除列表中選中的步驟。
  * **上移 / 下移**：調整步驟執行順序。
  * **轉存為通用腳本**：將設定好的進階腳本儲存為 JSON，自動切換至基本功能分頁準備播放。

---

### 3.3 【技能套組設定】技能連招與 CD 偵測
專為 ARPG / 戰鬥類遊戲設計的技能自動化系統：

* **技能預設檔編輯**：
  * 可儲存、載入自訂的技能套組（存於 `presets/` 資料夾）。
  * 支援簡潔的自訂語法（如 `剣姬123a45:4:4:4b`）。
* **技能位置與 CD 校正**：
  * 設定技能圖標（1~6, a~f）在模擬器畫面上的相對座標。
  * 支援設定亮度閾值（判斷技能亮起/冷卻中）。
* **語法即時預覽與試射**：
  * 點擊「解析並預覽」可確認連招組別與等待時間。
  * 提供「單次試射」測試背景傳送技能點擊是否正確。

---

### 3.4 【多開總控】多模擬器集中管理
當同時開啟多個模擬器時，本分頁提供總覽與集中控制：

* **多開表格檢視**：顯示每一個模擬器的 PID、HWND 句柄、目前載入腳本與運行狀態（空閒/播放中/錄製中）。
* **個別控制與批次控制**：
  * 可為不同模擬器指定不同的 JSON 腳本。
  * 支援「全選」、「批次啟動選取視窗」、「批次停止選取視窗」。

---

## 4. ⚡ 技能指令語法 (Skill DSL) 詳細說明

技能套組採用輕量化語法，方便快速撰寫複雜連招：

### 符號說明
| 符號 | 功能說明 | 範例 |
| :--- | :--- | :--- |
| `@` | 分割不同角色/職業的「套組」 | `@剣姬... @狂怒...` |
| `:` | 分割「同連招內的技能組」（依序連續施放） | `123a:4:4:4` |
| `-數字` | 指定該組技能施放完成後的「等待延遲秒數」 | `12345-30` (施放12345後等待30秒) |
| `1` ~ `6` | 底部技能/武器槽技能 ID | `1`, `2`, `3`, `4`, `5`, `6` |
| `a` ~ `f` | 頂部角色/同伴/特技技能 ID | `a`, `b`, `c`, `d`, `e`, `f` |

### 語法範例
1. `剣姬123a45:4:4:4b`
   * 套組名稱：`剣姬`
   * 第一組：順序按下技能 1, 2, 3, a, 4, 5
   * 第二至四組：連續按技能 4
   * 第五組：按下技能 4 與 b
2. `@狂怒-20:1a:12345-30:ef`
   * 第一組：等待 20 秒
   * 第二組：按下 1, a
   * 第三組：按下 1, 2, 3, 4, 5 並等待 30 秒
   * 第四組：按下 e, f

---

## 5. 🧩 進階腳本 11 種步驟類型說明

在【進階腳本】中，您可以組合以下 11 種功能步驟：

1. **點擊 (Click)**：在指定座標 $(x, y)$ 發送背景點擊。
2. **滑動 (Swipe)**：從 $(s\_x, s\_y)$ 滑動至 $(e\_x, e\_y)$，可設定持續時間。
3. **等候 (Wait)**：暫停執行 $N$ 秒。
4. **找圖點擊 (Find&Click)**：於模擬器畫面搜尋特定圖片（如確認按鈕），找到時自動點擊目標。
5. **找圖跳轉 (Find&Jump)**：搜尋圖片，根據「有找到」或「未找到」分支跳轉至指定步驟行數。
6. **結束關卡 (Check Finish)**：自動檢查是否有關卡結束/結算圖標，無圖標時跳轉重試。
7. **智能結算 (Smart Finish)**：連續循環點擊指定結算位置，直到終點畫面出現或逾時。
8. **等待進入戰鬥 (Wait Battle Start)**：阻塞等待直到畫面出現戰鬥計時器。可設定偵測秒數、超時/非戰鬥時跳轉步數，以及跳轉模式（R相對/A絕對/N不跳轉），同時涵蓋了快速偵測與長期等待兩大場景。
9. **等待戰鬥結束 (Wait Battle End)**：阻塞等待直到戰鬥計時器消失（戰鬥結束）。
10. **戰鬥技能 (Combat Skill)**：進入戰鬥狀態並自動啟用 SkillPreset 自動發動技能與 CD 判讀。

---

## 6. 🚀 多開啟動器 (start_bots.py) CLI 使用指南

針對大量多開與伺服器自動化，系統提供 Python 命令行啟動器 `start_bots.py`。

### 設定多開腳本配置
修改 `start_bots.py` 裡面的 `configs` 陣列：
```python
configs = [
    {"index": 0, "script": "活動後篇2(古代).json", "title": "古代千影-Bot"},
    {"index": 1, "script": "活動後篇2(亡命).json", "title": "亡命千影-Bot"},
    {"index": 2, "script": "活動後篇2(劍姬).json", "title": "劍姬千影-Bot"},
]
```

### 執行啟動器
在命令提示字元 (CMD) 或 PowerShell 中執行：
```bash
python start_bots.py
```
* 系統會詢問要啟動幾個視窗（輸入數字或直接按 Enter 全部啟動）。
* 啟動器會自動為每一個分身彈出獨立的 Console 視窗，載入對應模擬器索引 (`--index`) 與腳本 (`--script`) 並自動背景掛機。

---

## 7. ❓ 常見問題與安全注意事項 (FAQ)

#### Q1: 點擊沒有反應怎麼辦？
* 請確認雷電模擬器視窗標題或類別名稱是否正確（預設相容 `LDPlayerMainFrame` 及標題含有 `LDPlayer` 或 `雷電模擬器` 的視窗）。
* 請確認模擬器並未以「管理者權限」執行而 Python 以「一般權限」執行；如有權限落差，請以管理者身份重新開啟 Python 介面。

#### Q2: 播放中如何緊急停止？
* 全域緊急中斷快捷鍵為 **`ESC`** 或 **`F12`** 鍵。
* 或可在 GUI 介面點擊 **「停止播放」** 按鈕。

#### Q3: 找圖比對失敗或找不到圖片？
* 建議在【技能套组】或【進階腳本】中將比對門檻 (Threshold) 稍微調低（如從 0.8 調至 0.7）。
* 請確保截圖檔案格式為 PNG 或 JPG，且圖片像素尺寸不要過大。

#### Q4: 多開背景運行時會干擾滑鼠嗎？
* 不會。本系統使用 Windows 原生 `PostMessage` 背景訊息傳送機制，不會移動實體滑鼠游標，您可以在掛機時繼續正常使用電腦。
